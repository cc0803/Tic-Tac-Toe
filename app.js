const Gameboard = (() => {
    let gameboardArray = [null, null, null, null, null, null, null, null, null];
    let markers = ["X", "O"];
})()

const Player = (m) => {
    const marker = m;
}

const Gameflow = () => {

}